/**
 * Used to make the will to spin the given
 * number.
 */
export const NUMBER_OF_SPINS = 8;

/**
 * The wheel is divided in 8 parts,
 * each portion is 45 degrees.
 * The half is this value.
 */
export const PORTION_ANGLE = 45;

/**
 * One spin is 360 degrees.
 */
export const FULL_SPIN_DEGREES = 360;

/**
 * Used to add the spinning animation,
 * once the keyframes are added.
 */
export const SPIN_ANIMATION_NAME = "spinWheel";
